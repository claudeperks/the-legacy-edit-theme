{% layout 'theme' %}

<div class="page-width section">
  <h1 class="section-title" style="margin-bottom:2rem;">{{ page.title }}</h1>
  <div class="rich-text__body" style="max-width:720px;">{{ page.content }}</div>
</div>
